all:
  children:
    target:
      hosts:
        target_server:
          ansible_host: 10.5.0.20
          ansible_user: vagrant
          ansible_become: true
          ansible_ssh_private_key_file: ~/.ssh/target
          
